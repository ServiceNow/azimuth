import React from "react";
import { Typography } from "@mui/material";

const Predictions = () => {
  return (
    <div>
      <Typography variant="h4">Error Analysis</Typography>
      <Typography variant="h5">Prediction Errors</Typography>
    </div>
  );
};

export default Predictions;

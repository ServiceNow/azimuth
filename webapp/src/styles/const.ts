export const HEADER_HEIGHT = 50;

// Those widths doesn't include the 1 px border around the filter container
export const FILTER_CONTAINER_OPENED_WIDTH = 420;
export const FILTER_CONTAINER_CLOSED_WIDTH = 36;

export const TOOLTIP_ENTER_DELAY = 500;
